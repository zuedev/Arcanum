// Re-export centralized configuration for backward compatibility
export {
  CONFIG,
  CURRENCY_TYPES,
  CURRENCY_ABBREVIATIONS,
  CURRENCY_TO_GOLD_CONVERSION,
  DEFAULT_CONVERSION_FEE,
  ERROR_MESSAGES
} from '../../../config/constants.js';